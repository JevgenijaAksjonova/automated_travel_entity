obstacle_detection:
    thresholds:
        HEIGHT_LOWER_THRESHOLD : 0.02
        HEIGHT_UPPER_THRESHOLD : 0.04
        DISTANCE_THRESHOLD : 0.4
        BIN_THRESHOLD : 100
    visual:
        PUBLISH_MARKERS : true